function getRandomInt(max) {
  return Math.floor(Math.random() * Math.floor(max));
}

// Example usage:

let randomNumber = getRandomInt(10);
console.log(randomNumber); // Output can vary between 1 and 9
